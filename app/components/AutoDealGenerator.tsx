'use client'

import React, { useEffect } from 'react'

export default function AutoDealGenerator() {
  // Temporarily disabled to prevent fetch errors during startup
  // The deals will be generated on-demand when users visit the deals page

  return null
}
